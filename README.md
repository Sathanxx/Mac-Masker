# 🔧 MAC Address Changer – Python

A simple and beginner-friendly **MAC Address Changer** tool built using Python.  
This tool allows you to temporarily change the MAC address of your network interface on Linux-based systems.

---

## ▶️ Usage
Run the script as **root** or with `sudo`:

```bash
sudo python3 mac_changer.py -i <interface> -m <new_mac>
```

### Example:
```bash
sudo python3 mac_changer.py -i wlan0 -m 11:22:33:44:55:66
```

---

## ⚠️ Disclaimer
Use only for **ethical and legal** purposes on networks/devices you own.
